/**
 * Scheduling configuration for Bryant Labs consultation flow.
 *
 * TODO: Store the Calendly event URL here (or via env) for complimentary intro calls.
 * TODO: After intake review, send the intro-call scheduling link automatically by email.
 * TODO: Add paid Calendly or Stripe-backed sessions for Discovery and Product Strategy.
 */

export const introCallTitle = "Free Intro Call"

export const introCallSchedulingCopy =
  "After your intake is reviewed, we'll email you a scheduling link for a complimentary intro call."

/** Shared by email after intake review — not linked publicly for self-serve booking yet. */
export const introCallCalendlyUrl = null
